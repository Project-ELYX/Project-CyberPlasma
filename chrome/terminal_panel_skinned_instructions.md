Wire-up notes:

Put Kitty (or your terminal widget) underneath and align to term-safe.

Overlay tab titles centered in each tab seat; recolor active tab via CSS class.

Bottom status: cwd left; right can show shell, git branch, SSH, etc.